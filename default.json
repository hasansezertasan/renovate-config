{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests"],
  "labels": ["internal"],
  "semanticCommits": "enabled",
  "dependencyDashboard": true,
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update rev pins for prek hooks (Renovate's built-in pre-commit manager only reads .pre-commit-config.yaml, not prek.toml)",
      "managerFilePatterns": ["/(^|/)prek\\.toml$/"],
      "matchStrings": [
        "repo\\s*=\\s*\"https://github\\.com/(?<depName>[^\"]+?)(?:\\.git)?\"\\s*\\n\\s*rev\\s*=\\s*\"(?<currentValue>[^\"]+)\""
      ],
      "datasourceTemplate": "github-tags"
    }
  ],
  "packageRules": [
    {
      "description": "Label and auto-merge green prek.toml hook rev updates",
      "matchManagers": ["custom.regex"],
      "matchFileNames": ["prek.toml", "**/prek.toml"],
      "commitMessageTopic": "prek hook {{depName}}",
      "automerge": true
    },
    {
      "description": "Auto-merge green GitHub Actions updates",
      "matchManagers": ["github-actions"],
      "automerge": true
    },
    {
      "description": "Never auto-merge Python version bumps (e.g. .python-version)",
      "matchDatasources": ["python-version"],
      "automerge": false
    }
  ]
}
